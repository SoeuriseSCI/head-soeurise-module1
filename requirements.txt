# Dépendances pour _Head.Soeurise Module 1
# Architecture Mémoire Hiérarchisée v2.0

# API Anthropic (Claude)
anthropic>=0.39.0

# Base de données PostgreSQL
psycopg2-binary>=2.9.0

# Requêtes HTTP (pour charger fichiers GitHub)
requests>=2.31.0

# Serveur web (pour garder le service actif)
flask>=3.0.0

# Les autres dépendances sont dans la bibliothèque standard Python :
# - imaplib (emails IMAP)
# - smtplib (envoi emails)
# - email (parsing emails)
# - json (parsing JSON)
# - datetime (gestion dates)
# - os (variables d'environnement)
